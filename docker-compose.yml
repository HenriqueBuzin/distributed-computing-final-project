version: "3.9"
services:
  web:
    build:
      context: server/
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./server/:/app
      - /var/run/docker.sock:/var/run/docker.sock
